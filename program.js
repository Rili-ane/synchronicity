// dem libraries
const Discord = require("discord.js");
const ms = require("ms");
const ontime = require('ontime')

//The bot itself
const client = new Discord.Client(); //<- IMPORTANT, NEVER REVEAL THE BOT TOKEN
client.login(process.env.BOT_TOKEN);

if (command == gulag)
